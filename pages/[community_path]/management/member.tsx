import React from 'react';
import { ManagerMember } from '../../../src/components';

const ManagerMemberPage = (): React.ReactElement => {
  return <ManagerMember />;
};

export default ManagerMemberPage;
